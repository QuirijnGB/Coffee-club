div.hero-unit
  p Next up is...
  h1 #{users[0].name}
  input.relieve(type="button",value="Relieve", data-id="#{users[0]._id}", data-times="#{users[0].timesBought}").btn.btn-large.btn-primary

p Want to tell the future?
table.table.table-striped.table-bordered(style="text-align:middle")
  tr
    th Name
    th Last bought on
    th Times bought
    th Something
    th Actions
  -each val, key in users
    tr
      td.span2 #{val.name}
      td.span2 #{val.date.toDateString() }
      td.span2 #{val.timesBought }
      td
        div.progress
          div.bar(style="width:10%")
      td.span1
        input.relieve(type="button",value="Relieve", data-id="#{val._id}", data-times="#{val.timesBought}").btn.btn-mini.btn-primary